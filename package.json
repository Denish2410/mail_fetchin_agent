{
  "name": "mail-fetching-agent",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "node -e \"const fs=require('fs'),cp=require('child_process'); if(fs.existsSync('client')) cp.execSync('npm run build --prefix client',{stdio:'inherit'}); else if(fs.existsSync('../client')) cp.execSync('npm run build --prefix ../client',{stdio:'inherit'});\""
  }
}
